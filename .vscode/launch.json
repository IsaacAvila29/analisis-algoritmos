{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Depurar archivo actual (lldb)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build: archivo actual",
            "terminal": "integrated"
        }
    ]
}
